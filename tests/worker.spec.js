const Worker = require('../src/libs/worker');

describe('Test worker pool', () => {

    let worker;

    beforeAll(() => {
        // worker = new Worker({ task, filePath , queue });
    });


    it('creates a worker instance', () => {
        // expect(worker instanceof Worker).toBe(true);
        // expect(!!worker.thread).toBe(true);
    });

});